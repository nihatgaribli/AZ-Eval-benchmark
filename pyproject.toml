[project]
name = "az-eval"
version = "0.1.0"
description = "Azərbaycan dili üçün açıq LLM qiymətləndirmə dəsti"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
